def birthdayCakeCandles(candles):
    tallest = max(candles)
    candles.sort(reverse = True)
    count = 0
    
    for i in candles:
        if i == tallest:
            count += 1
        else :
            break
    
    return count
